{%- comment -%}

This snippet renders a list item with all tags listed out.
It is used in article.liquid and blog.liquid.

More information:
- index#article-tags
- http://docs.shopify.com/themes/liquid-variables/article

{%- endcomment -%}

{%- if article.tags.size > 0 -%}

    <div class="tags">

        <span class="mr-1">{{ 'blogs.article.tags' | t }}:</span>

        {%- for tag in article.tags -%}

            <a class="text-bold" href="{{ blog.url }}/tagged/{{ tag | handle }}">{{ tag }}</a>

            {%- unless forloop.last -%}

            <span class="meta-sep text-light mr-1">,</span>

            {%- endunless -%}

        {%- endfor -%}

    </div>
{%- endif -%}